# fs/

Empty on purpose. Per docs/roadmap.md, the VFS node tree lands in Phase 2
(basic VFS node tree) and gets fleshed out in Phase 5 (VFS nodes, RAM
filesystem, initramfs) and Phase 8 (block device API, FAT32). Nothing to
build here yet - this file just keeps the directory tracked in git.
